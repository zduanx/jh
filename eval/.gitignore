# Captured eval data — generated by capture.js, contains run-specific answers.
cases.captured.json

# Isolated Python venv for the Ragas runner (pinned ragas 0.2.x deps). Never commit a venv.
.venv-ragas/
