# ============================================================================
# BACKEND ENVIRONMENT VARIABLES - TEMPLATE
# ============================================================================
#
# This is a template for .env.local (which is gitignored)
#
# For AWS Lambda deployment:
#   - Production values: Use VARIABLE_NAME_PROD_VALUE comments
#   - CloudFormation metadata: PARAM_NAME, DESCRIPTION, NO_ECHO, DEFAULT
#   - Deployment config: See .sam-config file
#   - Python scripts auto-generate template.yaml and samconfig.toml
#
# Format for each parameterized variable:
#   VARIABLE_NAME=local_value
#   # VARIABLE_NAME_PROD_VALUE=production_value
#   # VARIABLE_NAME_PARAM_NAME=CloudFormationParameterName
#   # VARIABLE_NAME_DESCRIPTION=Description for CloudFormation
#   # VARIABLE_NAME_NO_ECHO=true|false (hide value in CloudFormation logs)
#   # VARIABLE_NAME_DEFAULT=default_value (optional)
#
# Static variables (same for all environments) are defined in .sam-config
# ============================================================================

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_ID_PROD_VALUE=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_ID_PARAM_NAME=GoogleClientId
# GOOGLE_CLIENT_ID_DESCRIPTION=Google OAuth Client ID
# GOOGLE_CLIENT_ID_NO_ECHO=false

# JWT Configuration
# Generate SECRET_KEY with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-use-openssl-rand-hex-32
# SECRET_KEY_PROD_VALUE=your-production-secret-key
# SECRET_KEY_PARAM_NAME=JWTSecretKey
# SECRET_KEY_DESCRIPTION=Secret key for JWT signing (use openssl rand -hex 32)
# SECRET_KEY_NO_ECHO=true

# CORS Configuration
# Comma-separated URLs allowed to access the API
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# ALLOWED_ORIGINS_PROD_VALUE=http://localhost:3000,https://your-vercel-app.vercel.app
# ALLOWED_ORIGINS_PARAM_NAME=AllowedOrigins
# ALLOWED_ORIGINS_DESCRIPTION=Comma-separated list of allowed CORS origins
# ALLOWED_ORIGINS_NO_ECHO=false
# ALLOWED_ORIGINS_DEFAULT=http://localhost:3000,http://localhost:5173

# Access Control
# Comma-separated email addresses allowed to authenticate
ALLOWED_EMAILS=your-email@gmail.com
# ALLOWED_EMAILS_PROD_VALUE=your-email@gmail.com
# ALLOWED_EMAILS_PARAM_NAME=AllowedEmails
# ALLOWED_EMAILS_DESCRIPTION=Comma-separated list of allowed user emails
# ALLOWED_EMAILS_NO_ECHO=false
# ALLOWED_EMAILS_DEFAULT=your-email@gmail.com

# Database Configuration (Neon PostgreSQL)
# Local: test/dev branch for safety
# Production: main branch (set via PROD_VALUE)
DATABASE_URL=postgresql://user:password@host-dev/neondb?sslmode=require
# DATABASE_URL_PROD_VALUE=postgresql://user:password@host-prod/neondb?sslmode=require
# DATABASE_URL_PARAM_NAME=DatabaseUrl
# DATABASE_URL_DESCRIPTION=PostgreSQL database connection URL (production branch)
# DATABASE_URL_NO_ECHO=true

# Test Database (for pytest with isolated transactions)
# Local: Same as DATABASE_URL (test/dev branch)
# Production: For testing SQS/Lambda with test data
TEST_DATABASE_URL=postgresql://user:password@host-dev/neondb?sslmode=require
# TEST_DATABASE_URL_PROD_VALUE=postgresql://user:password@host-dev/neondb?sslmode=require
# TEST_DATABASE_URL_PARAM_NAME=TestDatabaseUrl
# TEST_DATABASE_URL_DESCRIPTION=PostgreSQL test database connection URL (for testing SQS/Lambda)
# TEST_DATABASE_URL_NO_ECHO=true
